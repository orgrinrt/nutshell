# Generated by nut-gen-resolver from lib.nut. Do not edit.
#
# The manifest is the source. This is the same rows as a `case`, so a
# lookup answers without reading a file. Regenerate when lib.nut moves;
# `check_resolver_current` fails when this and the manifest disagree.

_nut_resolve() {
    _nr_root="${2:-$NUTSHELL_ROOT}"
    _nr_reach="${3:-any}"
    case "$1" in
        array)
            { printf '%s' "$_nr_root/lib/array.sh"; return 0; }
            ;;
        attr)
            { printf '%s' "$_nr_root/lib/attr.sh"; return 0; }
            ;;
        bench)
            { printf '%s' "$_nr_root/lib/bench.sh"; return 0; }
            ;;
        check-runner)
            { printf '%s' "$_nr_root/lib/check-runner.sh"; return 0; }
            ;;
        checkcache)
            { printf '%s' "$_nr_root/lib/checkcache.sh"; return 0; }
            ;;
        cli)
            { printf '%s' "$_nr_root/lib/cli.sh"; return 0; }
            ;;
        color)
            { printf '%s' "$_nr_root/lib/color.sh"; return 0; }
            ;;
        deps)
            { printf '%s' "$_nr_root/lib/deps.sh"; return 0; }
            ;;
        extern)
            { printf '%s' "$_nr_root/lib/extern.sh"; return 0; }
            ;;
        fs)
            { printf '%s' "$_nr_root/lib/fs.sh"; return 0; }
            ;;
        fs::impl::perl_stat)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'fs::impl::perl_stat' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/fs/impl/perl_stat.sh"; return 0; }
            ;;
        fs::impl::stat_bsd)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'fs::impl::stat_bsd' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/fs/impl/stat_bsd.sh"; return 0; }
            ;;
        fs::impl::stat_gnu)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'fs::impl::stat_gnu' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/fs/impl/stat_gnu.sh"; return 0; }
            ;;
        git)
            { printf '%s' "$_nr_root/lib/git.sh"; return 0; }
            ;;
        github)
            { printf '%s' "$_nr_root/lib/github.sh"; return 0; }
            ;;
        inuse)
            { printf '%s' "$_nr_root/lib/inuse.sh"; return 0; }
            ;;
        http)
            { printf '%s' "$_nr_root/lib/http.sh"; return 0; }
            ;;
        key)
            { printf '%s' "$_nr_root/lib/key.sh"; return 0; }
            ;;
        json)
            { printf '%s' "$_nr_root/lib/json.sh"; return 0; }
            ;;
        json::impl::jq)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'json::impl::jq' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/json/impl/jq.sh"; return 0; }
            ;;
        json::impl::perl)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'json::impl::perl' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/json/impl/perl.sh"; return 0; }
            ;;
        json::impl::python)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'json::impl::python' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/json/impl/python.sh"; return 0; }
            ;;
        log)
            { printf '%s' "$_nr_root/lib/log.sh"; return 0; }
            ;;
        list)
            _nut_gate 'shell(bash4)' && _nut_gate 'feature(bash)' && { printf '%s' "$_nr_root/lib/list.bash.sh"; return 0; }
            { printf '%s' "$_nr_root/lib/list.sh"; return 0; }
            ;;
        map)
            _nut_gate 'shell(bash4)' && _nut_gate 'feature(bash)' && { printf '%s' "$_nr_root/lib/map.bash.sh"; return 0; }
            { printf '%s' "$_nr_root/lib/map.sh"; return 0; }
            ;;
        modgraph)
            { printf '%s' "$_nr_root/lib/modgraph.sh"; return 0; }
            ;;
        nutshell)
            { printf '%s' "$_nr_root/nutshell.sh"; return 0; }
            ;;
        os)
            { printf '%s' "$_nr_root/lib/os.sh"; return 0; }
            ;;
        priv)
            { printf '%s' "$_nr_root/lib/priv.sh"; return 0; }
            ;;
        prompt)
            _nut_gate 'shell(bash4)' && _nut_gate 'feature(bash)' && { printf '%s' "$_nr_root/lib/prompt.sh"; return 0; }
            ;;
        srcfile)
            { printf '%s' "$_nr_root/lib/srcfile.sh"; return 0; }
            ;;
        string)
            _nut_gate 'shell(bash4)' && _nut_gate 'feature(bash)' && { printf '%s' "$_nr_root/lib/string.sh"; return 0; }
            { printf '%s' "$_nr_root/lib/string.posix.sh"; return 0; }
            ;;
        test)
            { printf '%s' "$_nr_root/lib/test.sh"; return 0; }
            ;;
        text)
            { printf '%s' "$_nr_root/lib/text.sh"; return 0; }
            ;;
        text::impl::awk_replace)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'text::impl::awk_replace' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/text/impl/awk_replace.sh"; return 0; }
            ;;
        text::impl::combo::grep_sed)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'text::impl::combo::grep_sed' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/text/impl/combo/grep_sed.sh"; return 0; }
            ;;
        text::impl::grep_match)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'text::impl::grep_match' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/text/impl/grep_match.sh"; return 0; }
            ;;
        text::impl::perl_match)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'text::impl::perl_match' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/text/impl/perl_match.sh"; return 0; }
            ;;
        text::impl::perl_replace)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'text::impl::perl_replace' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/text/impl/perl_replace.sh"; return 0; }
            ;;
        text::impl::sed_replace)
            { [ "$_nr_reach" = public ] && { echo "nutshell: 'text::impl::sed_replace' is internal to that library" >&2; return 1; }; printf '%s' "$_nr_root/lib/text/impl/sed_replace.sh"; return 0; }
            ;;
        toml)
            { printf '%s' "$_nr_root/lib/toml.sh"; return 0; }
            ;;
        toml::json)
            { printf '%s' "$_nr_root/lib/toml/json.sh"; return 0; }
            ;;
        toml::write)
            { printf '%s' "$_nr_root/lib/toml/write.sh"; return 0; }
            ;;
        validate)
            { printf '%s' "$_nr_root/lib/validate.sh"; return 0; }
            ;;
        xdg)
            { printf '%s' "$_nr_root/lib/xdg.sh"; return 0; }
            ;;
    esac
    return 1
}
